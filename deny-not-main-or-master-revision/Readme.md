# Deny when revision is not main or master

By default, the policy will allow any deployment to be deployed. However, the policy will deny any deployment that is not from the `main` or `master` branch.
